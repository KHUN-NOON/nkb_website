import type { mainroutesTypes } from "@/types/routes/mainroutes.types"

const mainroutes: mainroutesTypes[] = [
    // { path: '/', title: 'Home' },
    { path: '/businesses', title: 'Businesses' },
    { path: '/about', title: 'About' },
    { path: '/contact', title: 'Contact' }
]

export default mainroutes